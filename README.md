# OhMyRevit

**OhMyRevit** — це сучасний маркетплейс для продажу та купівлі архівів Revit, глибоко інтегрований з **Telegram Web App**. Платформа надає користувачам зручний доступ до унікального контенту, систему бонусів, підписок та можливості для авторів монетизувати свою роботу.

---

## 📋 Зміст

1.  [**🚀 Про проєкт**](#-про-проєкт)
2.  [**📂 Структура проєкту**](#-структура-проєкту)
    * [Backend](#backend)
    * [Frontend](#frontend)
    * [Nginx](#nginx)
    * [Docker](#docker)
3.  [**⚙️ Опис файлів**](#️-опис-файлів)
    * [Коренева директорія](#коренева-директорія)
    * [Backend (`revit-store/backend`)](#backend-revit-storebackend)
    * [Frontend (`revit-store/frontend`)](#frontend-revit-storefrontend)
4.  [**💻 Опис функцій**](#-опис-функцій)
    * [Backend (Python/FastAPI)](#backend-pythonfastapi)
    * [Frontend (JavaScript)](#frontend-javascript)

---

## 🚀 Про проєкт

Платформа **OhMyRevit** складається з трьох основних компонентів:

* **Backend**: Написаний на Python з використанням фреймворку FastAPI. Відповідає за всю бізнес-логіку, роботу з базою даних (PostgreSQL), автентифікацію користувачів через Telegram, обробку платежів та керування файлами на S3-сумісному сховищі.
* **Frontend**: Написаний на нативному JavaScript, HTML та CSS. Це клієнтська частина, що працює як Telegram Web App. Відповідає за інтерфейс користувача, відображення товарів, кошик, профіль та взаємодію з Backend API.
* **Nginx**: Веб-сервер, який виступає як реверс-проксі для Backend та роздає статичні файли Frontend.

Все це об'єднано за допомогою **Docker** та **Docker Compose** для легкої розробки, тестування та розгортання.

---

## 📂 Структура проєкту

```
clode-revit-store/
├── .gitignore
├── README.md
└── revit-store/
    ├── backend/
    │   ├── alembic/                 # Скрипти для міграцій бази даних (Alembic)
    │   ├── app/
    │   │   ├── models/              # Моделі даних SQLAlchemy
    │   │   ├── routers/             # API ендпоінти (роутери)
    │   │   ├── services/            # Сервісний шар (бізнес-логіка)
    │   │   ├── utils/               # Допоміжні утиліти (напр. безпека)
    │   │   ├── database.py          # Налаштування з'єднання з БД
    │   │   └── main.py              # Головний файл FastAPI додатку
    │   ├── alembic.ini              # Конфігурація Alembic
    │   ├── Dockerfile               # Dockerfile для збірки backend-образу
    │   ├── init_db.py               # Скрипт для початкової ініціалізації БД
    │   └── requirements.txt         # Залежності Python
    ├── frontend/
    │   ├── assets/
    │   │   └── locales/             # Файли локалізації (en, ru, uk)
    │   ├── css/                     # Стилі CSS
    │   ├── js/
    │   │   ├── components/          # Окремі UI компоненти (Колесо фортуни)
    │   │   ├── core/                # Ядро frontend (API клієнт, утиліти)
    │   │   └── modules/             # Основні модулі (авторизація, товари, кошик і т.д.)
    │   ├── index.html               # Головна HTML сторінка
    │   ├── Dockerfile               # Dockerfile для збірки frontend-образу
    │   ├── manifest.json            # Маніфест PWA
    │   └── service-worker.js        # Service Worker для кешування
    ├── nginx/
    │   └── nginx.conf               # Конфігурація Nginx
    └── docker-compose.yml           # Файл для оркестрації контейнерів
```

---

## ⚙️ Опис файлів

### Коренева директорія

* `.gitignore`: Список файлів та директорій, які ігноруються системою контролю версій Git.
* `README.md`: Цей файл. Загальна документація по проєкту.
* `docker-compose.yml`: Визначає сервіси, мережі та вольюми для запуску всього проєкту за допомогою однієї команди. Об'єднує `backend`, `frontend`, `db` (PostgreSQL) та `nginx`.

### Backend (`revit-store/backend`)

* `Dockerfile`: Інструкції для створення Docker-образу Python-додатку. Встановлює залежності з `requirements.txt` та запускає `uvicorn`.
* `alembic.ini`: Файл конфігурації для інструменту міграцій бази даних Alembic.
* `alembic/`: Директорія, що містить самі міграції та середовище для їх запуску.
* `init_db.py`: Скрипт для створення початкових даних в базі, наприклад, створення адміністратора.
* `requirements.txt`: Перелік всіх Python-бібліотек, необхідних для роботи backend.
* **`app/main.py`**: Головний файл додатку. Ініціалізує FastAPI, підключає роутери, налаштовує CORS та middleware.
* **`app/database.py`**: Налаштовує з'єднання з базою даних PostgreSQL за допомогою SQLAlchemy.
* **`app/models/`**: Директорія з моделями даних. Кожен файл (`user.py`, `product.py` і т.д.) описує таблицю в базі даних через класи SQLAlchemy.
* **`app/routers/`**: Директорія, де кожен файл відповідає за групу API-ендпоінтів. Наприклад, `products.py` містить ендпоінти для отримання, створення, оновлення товарів.
* **`app/services/`**: Містить бізнес-логіку, винесену з роутерів. Наприклад, `payment_service.py` інкапсулює логіку взаємодії з платіжною системою, а `s3_service.py` — з файловим сховищем.
* **`app/utils/`**: Допоміжні функції, наприклад, `security.py` для хешування паролів та роботи з JWT токенами.

### Frontend (`revit-store/frontend`)

* `Dockerfile`: Створює Docker-образ на основі `nginx`, копіюючи в нього всі статичні файли (HTML, CSS, JS).
* `index.html`: Єдина HTML-сторінка додатку (Single Page Application). Всі динамічні зміни контенту відбуваються за допомогою JavaScript.
* `css/`: Директорія зі стилями. `styles.css` — основні стилі, інші файли — для окремих компонентів.
* `assets/locales/`: JSON-файли з перекладами інтерфейсу для підтримки багатомовності.
* **`js/app.js`**: Головний скрипт. Ініціалізує додаток, налаштовує роутинг, викликає функції з модулів для відображення відповідної сторінки.
* **`js/core/api.js`**: Централізований модуль для відправки запитів на backend. Містить функції-обгортки над `fetch` для всіх API-ендпоінтів.
* **`js/core/utils.js`**: Набір допоміжних функцій, що використовуються в різних частинах додатку (наприклад, форматування дати, показ сповіщень).
* **`js/modules/`**: Кожен файл є окремим модулем, що відповідає за певну частину функціоналу:
    * `auth.js`: Логіка реєстрації та авторизації.
    * `products.js`: Відображення списку товарів та деталей товару.
    * `cart.js`: Робота з кошиком (додавання, видалення, оформлення замовлення).
    * `admin.js`: Функціонал адмін-панелі.
    * `creator.js`: Функціонал для авторів контенту.
    * ... та інші.
* `service-worker.js` та `manifest.json`: Забезпечують роботу додатку як Progressive Web App (PWA), дозволяючи кешувати ресурси та встановлювати додаток на пристрій.

---

## 💻 Опис функцій

### Backend (Python/FastAPI)

#### `app/main.py`
* **`startup`**: Асинхронна функція, що виконується при старті серверу. Ініціалізує з'єднання з S3 та інші необхідні сервіси.
* **`shutdown`**: Виконується при зупинці серверу, закриває з'єднання.

#### `app/routers/auth.py`
* **`login`**: `POST /auth/login` - Приймає `initData` від Telegram, валідує їх, знаходить або створює користувача в БД та повертає JWT токен доступу.
* **`get_me`**: `GET /auth/me` - Повертає інформацію про поточного залогіненого користувача на основі JWT токену.

#### `app/routers/products.py`
* **`read_products`**: `GET /products` - Повертає список всіх товарів з можливістю фільтрації та пагінації.
* **`read_product`**: `GET /products/{product_id}` - Повертає детальну інформацію про конкретний товар.
* **`create_product`**: `POST /products` (для авторів/адмінів) - Створює новий товар, завантажує файли (прев'ю, архів) на S3.
* **`download_product_file`**: `GET /products/{product_id}/download` - Генерує та повертає тимчасове посилання на завантаження файлу товару з S3 для користувачів, які його придбали.

#### `app/routers/orders.py`
* **`create_order`**: `POST /orders` - Створює нове замовлення в БД на основі товарів у кошику користувача.
* **`create_payment_invoice`**: `POST /orders/{order_id}/pay` - Створює інвойс для оплати через платіжний сервіс (напр. YooKassa) та повертає посилання на оплату.
* **`payment_webhook`**: `POST /orders/webhook` - Ендпоінт для отримання сповіщень від платіжної системи про статус платежу. При успішній оплаті надає користувачу доступ до куплених товарів.

#### `app/services/telegram_auth.py`
* **`validate_init_data`**: Перевіряє хеш `initData`, отриманий від Telegram, щоб переконатися, що дані не були підроблені. Це ключова функція для безпечної автентифікації.

#### `app/services/payment_service.py`
* **`create_invoice`**: Створює об'єкт платежу, відправляє запит до API платіжної системи та повертає URL для редиректу користувача на сторінку оплати.

#### `app/services/s3_service.py`
* **`upload_file`**: Завантажує файл (байтовий потік) у вказаний бакет на S3-сховищі.
* **`generate_presigned_url`**: Створює тимчасове, підписане посилання для доступу до приватного файлу в S3, яке діє обмежений час.

### Frontend (JavaScript)

#### `js/app.js`
* **`router`**: Основна функція-маршрутизатор. Аналізує URL (`window.location.hash`) і викликає відповідну функцію для рендерингу сторінки (наприклад, `showProductsPage`, `showProfilePage`).
* **`init`**: Ініціалізує додаток при завантаженні сторінки: налаштовує обробники подій для навігації, ініціалізує Telegram Web App SDK, запускає `router`.

#### `js/core/api.js`
* **`request(endpoint, options)`**: Універсальна функція для виконання `fetch`-запитів. Автоматично додає `Authorization` заголовок з JWT токеном, обробляє відповіді та помилки.
* **`login(initData)`**: Викликає `request` для ендпоінту `/auth/login`.
* **`getProducts()`**: Викликає `request` для ендпоінту `/products`.
* ... та інші функції для кожного ендпоінту backend'у.

#### `js/modules/auth.js`
* **`performLogin()`**: Отримує `initData` від Telegram (`window.Telegram.WebApp.initData`), відправляє їх на бекенд через `api.login()`, зберігає отриманий токен в `localStorage` і оновлює інтерфейс.

#### `js/modules/products.js`
* **`displayProducts(products)`**: Приймає масив товарів, генерує для кожного HTML-розмітку (картку товару) і вставляє її в DOM.
* **`fetchAndShowProducts()`**: Використовує `api.getProducts()` для отримання списку товарів з бекенду, після чого викликає `displayProducts` для їх відображення.
* **`showProductDetails(productId)`**: Отримує дані про один товар, рендерить сторінку з детальним описом, зображеннями та кнопкою "Додати в кошик".

#### `js/modules/cart.js`
* **`addToCart(productId)`**: Додає ID товару до масиву кошика в `localStorage` та оновлює іконку кошика.
* **`renderCartPage()`**: Рендерить сторінку кошика: отримує повну інформацію про товари в кошику з бекенду, відображає їх, розраховує загальну суму.
* **`handleCheckout()`**: Створює замовлення через `api.createOrder()`, потім отримує посилання на оплату через `api.createInvoice()` і перенаправляє користувача на сторінку оплати за допомогою `window.Telegram.WebApp.openInvoice()`.

#### `js/components/wheel-of-fortune.js`
* **`initWheel()`**: Ініціалізує компонент "Колесо фортуни": рендерить колесо, налаштовує логіку обертання.
* **`spinWheel()`**: Відправляє запит на бекенд для отримання результату обертання, запускає анімацію обертання колеса і після зупинки показує результат (виграний бонус).
